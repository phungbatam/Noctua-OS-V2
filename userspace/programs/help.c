#include <stdio.h>

int main(void) {
    puts("Noctua OS v2.0 - Real OS Edition");
    puts("Commands:");
    puts("  echo       - Print text");
    puts("  cat        - Display file contents");
    puts("  ls         - List directory");
    puts("  clear      - Clear screen");
    puts("  help       - Show this help");
    puts("  exit       - Exit shell");
    puts("  cd         - Change directory");
    puts("  mkdir      - Create directory");
    puts("  rm         - Remove file");
    puts("  uname      - System info");
    puts("  reboot     - Reboot system");
    puts("Shell features: | pipes, > >> < redirection, & background");
    return 0;
}
